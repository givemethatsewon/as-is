# PRD: 실무 핵심 업무 흐름

**Product:** 처음처럼 - 원상태수출관리 / As-Is  
**Document Type:** Core Workflow PRD  
**Author Intent:** 실무자가 수입/수출 파일을 올리고, 오류를 확인하고, 자동 매칭 후 결과를 내려받고, 잘못 반영한 파일을 취소하는 핵심 흐름을 명확하게 정의한다.

---

## 1. 배경

실무자가 이 제품에서 기대하는 가치는 복잡한 분석 화면이 아니라 `업로드 -> 확인 -> 반영 -> 매칭 -> 다운로드 -> 필요 시 취소` 흐름을 안정적으로 처리하는 것이다.

현재 제품은 이 흐름을 이미 상당 부분 갖추고 있지만, 개발자 입장에서는 어떤 동작이 핵심 요구사항이고 어떤 예외가 반드시 보장되어야 하는지 문서로 분리돼 있으면 구현과 QA가 더 쉬워진다.

이번 PRD는 아래 5개 기능을 제품의 핵심 업무 흐름으로 정의한다.

1. 업로드 후 미리보기
2. 중복/충돌/오류 분류
3. FIFO, 360일 조건, 재고 부족 판정
4. 저장 후 매칭 결과 다운로드
5. 잘못 올린 파일 무효 처리

---

## 2. 목표

- 실무자가 파일 반영 전에 문제를 미리 확인할 수 있게 한다.
- 시스템이 수입/수출 데이터를 일관된 규칙으로 자동 판정하게 한다.
- 사용자는 매칭 결과를 바로 제출용/공유용 파일로 받을 수 있어야 한다.
- 잘못 반영된 파일은 이력은 남기되, 이후 집계와 결과에서 제외할 수 있어야 한다.

### 비목표

- 세관 외부 시스템 연동
- 자동 보정 추천
- 수기 수정 화면 추가
- 로그인/권한 체계 설계
- 매칭 규칙의 AI 자동 판단

---

## 3. 대상 사용자

- 수출입/관세환급 실무 담당자
- 업로드 파일 단위로 업무를 처리하는 사용자
- 파일별 반영 여부와 매칭 근거를 명확히 남겨야 하는 사용자

---

## 4. 핵심 사용자 시나리오

### 시나리오 1: 정상 처리

1. 사용자가 수입 파일을 업로드한다.
2. 시스템이 행별로 읽고 미리보기 결과를 보여준다.
3. 사용자는 오류 여부와 신규/중복/충돌 여부를 확인한다.
4. 사용자가 반영을 실행한다.
5. 사용자가 수출 파일도 같은 방식으로 업로드하고 반영한다.
6. 사용자가 매칭을 실행한다.
7. 사용자가 결과 파일을 다운로드한다.

### 시나리오 2: 문제 파일 처리

1. 사용자가 잘못된 형식 또는 값이 있는 파일을 업로드한다.
2. 시스템이 오류/충돌을 보여준다.
3. 사용자는 반영하지 않고 업로드를 취소하거나 파일을 수정해 다시 업로드한다.

### 시나리오 3: 반영 후 취소

1. 사용자가 이미 반영한 파일이 잘못된 것을 발견한다.
2. 사용자가 해당 업로드를 반영 취소한다.
3. 시스템은 해당 파일에서 생성된 본 데이터와 관련 집계 반영을 제외한다.
4. 이후 화면, 재고, 매칭 결과, 다운로드 파일에서도 제외된 상태가 반영된다.

---

## 5. 기능 요구사항

### 5.1 업로드 후 미리보기

#### 목적

사용자가 본 반영 전에 파일이 어떻게 인식됐는지 확인하게 한다.

#### 요구사항

- 수입 파일과 수출 파일 모두 업로드 직후 미리보기 화면이 제공되어야 한다.
- 시스템은 파일을 바로 본 데이터에 반영하지 않고, 먼저 미리보기 배치로 저장해야 한다.
- 미리보기에는 최소 아래 정보가 포함되어야 한다.
  - 업로드 파일 종류
  - 총 행 수
  - 행별 상태
  - 행별 안내 메시지
  - 시스템이 인식한 저장 예정 값
  - 파일 컬럼 인식 결과
- 사용자는 미리보기 화면에서 아래 중 하나를 선택할 수 있어야 한다.
  - 반영하기
  - 업로드 취소
  - 목록으로 돌아가기

#### 동작 원칙

- 업로드 직후에는 본 재고/수출 데이터에 영향을 주지 않아야 한다.
- 오류가 있는 경우 즉시 원인을 보여줘야 한다.
- 업로드 원본 컬럼명이 시스템 필드에 어떻게 연결됐는지 확인 가능해야 한다.

#### 수용 기준

- 업로드만으로는 실제 재고나 수출 요구 데이터가 생성되지 않는다.
- 미리보기 화면 없이 바로 반영되는 경로가 없어야 한다.
- 사용자는 각 행이 왜 정상/중복/충돌/오류로 분류됐는지 확인할 수 있다.

---

### 5.2 중복/충돌/오류 분류

#### 목적

사용자가 반영 전 데이터 품질을 빠르게 판단하게 한다.

#### 상태 정의

- `신규`
  - 기존 본 데이터에 없는 새 행
- `중복`
  - 기존 데이터와 사업 키와 값이 모두 동일한 행
- `충돌`
  - 기존 데이터와 사업 키는 같지만 값이 다른 행
- `오류`
  - 필수값 누락, 날짜 형식 오류, 수량 오류 등으로 정상 반영할 수 없는 행
- `재활성화`
  - 과거 무효 처리된 동일 수입 건을 새 업로드로 다시 살리는 행
  - 수입 파일에만 적용 가능

#### 요구사항

- 수입 파일은 최소 `신규`, `중복`, `충돌`, `오류`, `재활성화`를 분류해야 한다.
- 수출 파일은 최소 `신규`, `오류`를 분류해야 한다.
- 수입 데이터의 중복/충돌 판정은 아래 사업 키 기준으로 수행한다.
  - 수입신고번호
  - 란번호
  - 행번호
  - 품번
  - 원산지
- 값 비교 시 최소 아래 항목 차이를 감지해야 한다.
  - 수리일
  - HS 코드
  - 수량
  - 수량 단위
- 오류가 1건이라도 있으면 해당 배치는 반영할 수 없어야 한다.

#### 사용자 노출 요구사항

- 미리보기 상단에 상태별 개수를 요약해서 보여준다.
- 각 행에는 상태와 사유를 함께 보여준다.
- 충돌과 오류는 사용자가 바로 눈치챌 수 있게 강조되어야 한다.

#### 수용 기준

- 동일한 사업 키 + 동일 값은 중복으로 분류된다.
- 동일한 사업 키 + 상이한 값은 충돌로 분류된다.
- 날짜/수량/필수값 오류는 오류로 분류된다.
- 무효 처리된 동일 수입 건이 다시 들어오면 재활성화 대상으로 분류할 수 있다.

---

### 5.3 FIFO, 360일 조건, 재고 부족 판정

#### 목적

수출 요청을 사람이 수기로 맞추지 않아도 일관된 규칙으로 수입 재고와 연결한다.

#### 매칭 규칙

- 품번이 같아야 한다.
- 원산지가 같아야 한다.
- 수입 수리일이 수출일보다 늦으면 안 된다.
- 수출일 기준 360일 이내 수입 건만 매칭 가능하다.
- 남은 수량이 0보다 커야 한다.
- 여러 후보가 있으면 먼저 들어온 수입 건부터 차감한다. (FIFO)
- HS 코드가 다르더라도 매칭은 진행할 수 있지만 경고를 남긴다.

#### 재고 상태 요구사항

- 남은 수량이 0이면 `소진` 상태가 된다.
- 수출일 기준 360일 초과면 `기한 초과`로 본다.
- 330일 이상 360일 이하면 `만료 예정` 상태로 표시할 수 있다.
- 충분히 남아 있으면 `사용 가능` 상태로 유지한다.

#### 수출 결과 상태 요구사항

- 필요한 수량을 모두 채우면 `매칭 완료`
- 일부만 채우면 `일부 매칭`
- 하나도 채우지 못하면 `재고 부족`
- 재실행 시 기존 매칭을 중복 생성하면 안 된다.

#### 분할 매칭 요구사항

- 하나의 수출 건은 여러 수입 건에 나누어 매칭될 수 있어야 한다.
- 하나의 수입 건도 잔량이 남아 있으면 여러 수출 건에 순차 차감될 수 있어야 한다.

#### 수용 기준

- 가장 오래된 적격 수입 건부터 차감된다.
- 360일 초과 수입 건은 매칭되지 않는다.
- 원산지가 다르면 매칭되지 않는다.
- 수량이 부족하면 일부 매칭 또는 재고 부족으로 정확히 판정된다.
- 같은 매칭 실행을 반복해도 이미 생성된 매칭이 중복 저장되지 않는다.
- HS 코드가 다르면 매칭은 생성되지만 경고가 남는다.

---

### 5.4 저장 후 매칭 결과 다운로드

#### 목적

실무자가 매칭 결과를 외부 공유, 검토, 제출 자료로 바로 활용하게 한다.

#### 요구사항

- 수입/수출 파일이 반영되고 매칭이 실행된 뒤, 사용자는 결과 파일을 다운로드할 수 있어야 한다.
- 최소 아래 결과는 제공되어야 한다.
  - 수출 건별 수입근거 매칭 결과
  - 수입신고별 잔량 현황
- 다운로드 형식은 현재 제품 정책에 맞춰 CSV 또는 XLSX를 제공할 수 있다.
- 결과 파일에는 무효 처리된 업로드에서 생성된 데이터가 포함되면 안 된다.
- 결과 파일에는 사용자가 매칭 근거를 추적할 수 있는 필드가 포함되어야 한다.
  - 수출일
  - 품번
  - 필요 수량
  - 매칭 수량
  - 수입신고번호
  - 수입수리일
  - 원산지
  - 수입 란/행
  - 매칭 후 잔량
  - 상태

#### 동작 원칙

- 반영 전 미리보기 데이터는 다운로드 결과에 포함되지 않는다.
- 다운로드는 현재 유효한 본 데이터 기준으로 생성된다.
- 무효 처리 후 다시 다운로드하면 변경된 결과가 반영되어야 한다.

#### 수용 기준

- 반영과 매칭이 끝난 뒤 사용자는 결과 파일을 받을 수 있다.
- 결과 파일만 봐도 어떤 수출 건이 어떤 수입 건과 연결됐는지 추적 가능하다.
- 무효 처리된 파일의 데이터는 다운로드에서 제외된다.

---

### 5.5 잘못 올린 파일 무효 처리

#### 목적

반영 완료된 잘못된 파일을 삭제하지 않고, 이력을 남긴 채 결과 계산에서 제외한다.

#### 요구사항

- 반영 전 파일은 삭제 또는 업로드 취소가 가능해야 한다.
- 반영 완료된 파일은 직접 삭제하지 않고 `무효 처리` 또는 이에 준하는 취소 동작으로 관리해야 한다.
- 무효 처리 시 해당 업로드에서 생성된 본 데이터는 이후 집계에서 제외되어야 한다.
- 수입 파일 무효 처리 시:
  - 해당 수입 건들과 연결된 매칭 결과를 제거해야 한다.
  - 영향받은 수출 건 상태를 다시 계산해야 한다.
- 수출 파일 무효 처리 시:
  - 해당 수출 건에 연결된 매칭을 제거해야 한다.
  - 관련 수입 재고 수량을 복원해야 한다.
  - 관련 lot 상태를 다시 계산해야 한다.
- 무효 처리된 파일은 이력 목록에 남아야 한다.
- 무효 처리 사유를 남길 수 있어야 한다.

#### 동작 원칙

- 물리 삭제보다 업무상 제외를 우선한다.
- 무효 처리 후 대시보드, 재고, 매칭 결과, 다운로드 파일 모두 일관되게 갱신되어야 한다.
- 이미 무효 처리된 파일은 다시 무효 처리할 수 없어야 한다.

#### 수용 기준

- 반영 완료 파일은 이력 보존 상태로 취소 가능하다.
- 무효 처리 후 관련 데이터가 집계와 결과에서 제외된다.
- 수입 파일 무효 처리와 수출 파일 무효 처리 모두 각각 올바른 역정산이 일어난다.
- 무효 처리된 파일은 사용자에게 상태가 명확히 보인다.

---

## 6. 예외 및 에러 처리

- 필수 컬럼 누락 시 업로드 단계에서 즉시 오류를 보여준다.
- 날짜 형식이 잘못되면 행 단위 오류로 처리한다.
- 수량이 0 이하이거나 숫자가 아니면 오류 처리한다.
- 잘못된 파일 종류를 다른 반영 API에 보내면 차단한다.
- 이미 반영한 배치를 다시 반영하려 하면 차단한다.
- 이미 무효 처리된 배치를 다시 무효 처리하려 하면 차단한다.
- 존재하지 않는 배치 ID 접근 시 오류를 반환한다.

---

## 7. 화면 및 상태 흐름 요약

### 업로드 배치 상태

- 반영 전
- 반영 완료
- 무효

### 수입 재고 상태

- 사용 가능
- 만료 예정
- 기한 초과
- 소진

### 수출 매칭 상태

- 매칭 대기
- 매칭 완료
- 일부 매칭
- 재고 부족
- 무효

---

## 8. QA 체크리스트

1. 업로드 후 바로 본 데이터가 생기지 않는가
2. 미리보기에서 상태별 개수가 맞는가
3. 중복/충돌/오류 분류가 사업 키 기준에 맞는가
4. 오류가 있으면 반영 버튼이 비활성화되는가
5. FIFO 순서대로 매칭되는가
6. 360일 초과 재고가 제외되는가
7. 재고 부족 시 일부 매칭/재고 부족 상태가 올바른가
8. 같은 매칭을 다시 실행해도 중복 매칭이 쌓이지 않는가
9. 다운로드 파일에서 매칭 근거 추적이 가능한가
10. 무효 처리 후 대시보드/재고/다운로드 결과가 함께 바뀌는가

---

## 9. 기술 메모

- 업로드는 본 데이터와 분리된 배치/미리보기 구조를 유지하는 것이 바람직하다.
- 상태값 내부 코드는 영문이어도 무방하지만, 사용자 노출 라벨은 일관되게 관리해야 한다.
- 무효 처리 시 물리 삭제보다 소프트 무효화 방식이 적합하다.
- 재고 수량, lot 상태, 수출 상태는 무효 처리 후 반드시 재계산 또는 역정산돼야 한다.

---

## 10. 완료 정의

아래를 모두 만족하면 이 PRD 범위가 완료된 것으로 본다.

1. 업로드 후 반드시 미리보기를 거친다.
2. 수입/수출 파일이 규칙에 맞게 상태 분류된다.
3. FIFO, 360일, 재고 부족 규칙으로 자동 매칭된다.
4. 반영 후 사용자가 매칭 결과를 다운로드할 수 있다.
5. 잘못 반영한 파일을 이력 보존 상태로 무효 처리할 수 있다.
6. 무효 처리 결과가 재고, 매칭, 리포트에 일관되게 반영된다.
