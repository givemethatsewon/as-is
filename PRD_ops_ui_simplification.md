# PRD: 실무자용 UI 단순화

**Product:** 처음처럼 - 원상태수출관리 / As-Is  
**Document Type:** UI/UX 개선 PRD  
**Author Intent:** 실무자 관점에서 자주 쓰는 기능만 전면에 두고, 헷갈리는 용어와 버튼 구성을 단순화한다.

---

## 1. 배경

현재 제품은 데모/설명 관점의 요소가 함께 들어 있어, 실무 사용자가 처음 접했을 때 다음과 같은 부담이 있다.

- 대시보드에 당장 업무 처리와 직접 연결되지 않는 정보가 함께 보인다.
- 다운로드 버튼이 여러 개여서 어떤 파일을 받아야 하는지 판단해야 한다.
- `검토`, `저장`, `무효 처리` 같은 표현이 시스템 관점이라 사용자가 결과를 직관적으로 예상하기 어렵다.

실무자의 핵심 업무는 아래 5가지다.

1. 파일 올리기
2. 오류 확인하기
3. 본 데이터에 반영하기
4. 자동 매칭 결과 확인하기
5. 제출용 또는 공유용 파일 내려받기

이번 개선은 위 5단계를 더 빠르고 덜 헷갈리게 만드는 데 집중한다.

---

## 2. 목표

### 목표

- 실무자가 첫 화면에서 필요한 정보만 빠르게 확인할 수 있게 한다.
- 다운로드 화면에서 기본 선택지를 하나로 줄여 의사결정 부담을 줄인다.
- 업무 용어를 더 쉬운 말로 바꿔 화면만 보고도 다음 행동을 이해할 수 있게 한다.

### 비목표

- 매칭 로직 변경
- 업로드 파일 포맷 변경
- API 응답 스키마 변경
- 다운로드 파일 내용 자체의 구조 변경
- 권한, 로그인, 감사 로그 기능 추가

---

## 3. 대상 사용자

- 수출입/관세환급 실무 담당자
- 시스템 구조보다 `지금 무엇을 눌러야 하는지`가 더 중요한 사용자
- 반복 업무에서 빠른 처리와 오류 방지가 중요한 사용자

---

## 4. 문제 정의

### 4.1 대시보드

현재 대시보드는 요약 수치, 만료 임박 재고, 매칭 상태 분포, HS 코드 확인 필요 테이블을 함께 보여준다.  
하지만 실무자 기준으로는 `지금 쓸 수 있는 재고`, `곧 만료되는 재고`, `남은 수량` 정도가 우선이며, 나머지는 상세 확인용 정보에 가깝다.

### 4.2 다운로드

현재 다운로드 화면에는 다음 3개 버튼이 동시에 보인다.

- 매칭 결과 CSV
- 공모전 양식 XLSX
- 전체 리포트 XLSX

실무자는 어떤 파일이 기본인지 즉시 알기 어렵다. 기본 출력물 1개를 명확히 보여주고, 나머지는 부가 옵션으로 내려야 한다.

### 4.3 용어

다음 표현은 내부 시스템에는 맞지만 실무자 입장에서는 덜 직관적이다.

- 파일 검토
- 검토 결과
- 저장
- 무효 처리
- 컬럼 연결
- 행별 결과
- 신규 / 충돌

---

## 5. 요구사항 요약

이번 PRD는 아래 3가지만 범위에 포함한다.

1. 대시보드 축소
2. 다운로드 버튼 단순화
3. 용어를 쉬운 말로 변경

---

## 6. 상세 요구사항

### 6.1 대시보드 축소

### 목적

대시보드를 `업무 우선순위 확인 화면`으로 재정의한다.

### 변경 요구사항

- 상단 요약 수치는 아래 4개만 기본 노출한다.
  - 남은 수량
  - 지금 매칭 가능한 재고
  - 30일 안에 기한이 지나는 건
  - 360일 초과 재고
- 아래 항목은 대시보드에서 제거한다.
  - 등록된 수입 수량
  - 수출 요청과 연결된 수량
  - 모두 사용한 수입 건
  - 매칭 상태 분포 패널
  - HS 코드 확인 필요 테이블
- `만료 임박 재고` 섹션은 유지한다.
- 대시보드 상단 설명 문구는 `중요한 재고 상태만 먼저 확인합니다.` 수준으로 단순화한다.

### 유지 조건

- 제거되는 정보는 데이터 집계 로직에서 완전히 삭제할 필요는 없다.
- 다른 화면이나 파일 다운로드에서 계속 사용할 수 있다.
- 이번 범위에서는 백엔드 API 축소보다 웹 UI 단순화를 우선한다.

### 수용 기준

- 대시보드 첫 화면에서 사용자가 5초 안에 핵심 재고 상태 4개를 구분할 수 있다.
- 대시보드에는 상세 분석 성격의 차트/분포/경고 테이블이 노출되지 않는다.
- 만료 임박 재고는 기존처럼 표 형태로 확인 가능하다.

---

### 6.2 다운로드 버튼 단순화

### 목적

사용자가 `무슨 파일을 받아야 하는지` 고민하지 않게 만든다.

### 변경 요구사항

- 다운로드 화면의 기본 CTA는 1개만 전면 배치한다.
  - 버튼명: `실무용 엑셀 다운로드`
- 이 기본 버튼은 기존 `전체 리포트 XLSX` 다운로드를 사용한다.
- 기존 `매칭 결과 CSV`, `공모전 양식 XLSX`는 삭제하지 않고 `추가 다운로드` 또는 `다른 형식 보기` 아래로 숨긴다.
- 숨김 방식은 아래 중 하나면 된다.
  - 접기/펼치기 섹션
  - 보조 링크 클릭 시 노출
  - 2차 버튼 영역
- 다운로드 화면 설명 문구는 기본 파일이 무엇인지 바로 이해되게 바꾼다.
  - 예: `실무에서 기본으로 쓰는 엑셀 파일을 먼저 제공합니다.`

### 유지 조건

- 기존 다운로드 API 엔드포인트는 유지한다.
- 공모전 양식과 CSV는 필요 시 계속 접근 가능해야 한다.
- 기능 제거가 아니라 기본 선택지 단순화가 목적이다.

### 수용 기준

- 화면 진입 시 가장 먼저 눈에 띄는 다운로드 버튼은 1개다.
- 사용자는 별도 설명 없이도 어떤 파일이 기본 파일인지 이해할 수 있다.
- 기존 부가 다운로드는 1번 이상 추가 클릭 후 접근 가능하다.

---

### 6.3 용어를 쉬운 말로 변경

### 목적

실무자가 시스템 용어를 해석하지 않고도 상태와 액션을 이해할 수 있게 한다.

### 원칙

- 동작 결과가 바로 떠오르는 말 사용
- `시스템 상태`보다 `업무 행동` 중심 표현 사용
- 한 화면 안에서는 같은 의미를 같은 단어로 통일

### 변경 대상 용어

| 현재 표현 | 변경 표현 | 비고 |
| --- | --- | --- |
| 파일 검토 | 파일 업로드 | 메뉴/페이지 제목 |
| 검토 결과 | 업로드 확인 | 상세 페이지 제목 |
| 최근 검토 파일 | 최근 업로드 파일 | 목록 제목 |
| 저장 | 반영하기 | 미리보기 데이터를 본 데이터에 넣는 행동 |
| 저장 완료 | 반영 완료 | 상태값 |
| 저장 전 | 반영 전 | 상태값 |
| 삭제 | 업로드 취소 | 반영 전 파일에만 사용 |
| 무효 처리 | 반영 취소 | 반영 완료 후 되돌리는 행동 |
| 컬럼 연결 | 파일 항목 인식 결과 | 사용자 설명 문구 |
| 행별 결과 | 업로드 결과 | 섹션 제목 |
| 신규 | 새로 반영 | 상태/카운트 |
| 중복 | 이미 있음 | 상태/카운트 |
| 충돌 | 값 다름 | 상태/카운트 |
| 오류 | 수정 필요 | 상태/카운트 |
| 수출 요청 | 수출 파일 | 업로드 화면에서는 더 쉬운 표현 우선 |
| 수입 데이터 | 수입 파일 | 업로드 화면에서는 더 쉬운 표현 우선 |

### 추가 문구 요구사항

- `반영 취소` 버튼 근처에는 짧은 안내 문구를 둔다.
  - 예: `이 파일로 반영된 재고/매칭 결과가 집계에서 제외됩니다.`
- `업로드 취소`는 반영 전 파일에서만 보이게 유지한다.
- `반영하기` 버튼은 `오류가 없을 때만 가능`하다는 점을 기존처럼 유지한다.

### 수용 기준

- 주요 액션 버튼은 `반영하기`, `업로드 취소`, `반영 취소` 3개 체계로 정리된다.
- 사용자는 버튼 이름만 보고 `지금 할 일`과 `되돌릴 수 있는 범위`를 추측할 수 있다.
- 같은 개념이 화면마다 다른 단어로 섞여 보이지 않는다.

---

## 7. 화면별 반영 범위

### 업로드 화면

- 페이지 제목 및 보조 문구 변경
- 최근 업로드 파일 섹션 명칭 변경
- 상태 라벨/버튼 라벨 변경

### 업로드 상세 화면

- 제목, 버튼, 상태 라벨 변경
- `컬럼 연결` 문구 변경
- `행별 결과` 문구 변경

### 대시보드

- 요약 카드 축소
- 매칭 상태 패널 제거
- HS 코드 확인 필요 테이블 제거
- 설명 문구 단순화

### 다운로드 화면

- 기본 CTA 1개로 재구성
- 부가 다운로드는 숨김 영역으로 이동
- 화면 설명 문구 변경

---

## 8. 사용자 스토리

### 스토리 1

실무자로서,  
첫 화면에서 중요한 재고 상태만 빠르게 보고 싶다.  
그래서 내가 당장 확인해야 할 위험과 사용 가능 재고를 바로 판단하고 싶다.

### 스토리 2

실무자로서,  
다운로드 화면에서 어떤 파일을 받아야 할지 고민하고 싶지 않다.  
그래서 기본 파일 하나만 바로 눌러서 업무를 이어가고 싶다.

### 스토리 3

실무자로서,  
버튼과 상태명이 쉬운 말로 쓰여 있으면 좋겠다.  
그래서 교육 없이도 `업로드`, `반영`, `취소` 흐름을 이해하고 싶다.

---

## 9. 측정 지표

정량 측정이 바로 어렵다면 아래 관찰 지표를 사용한다.

- 첫 사용자가 다운로드 화면에서 버튼을 망설이는 횟수 감소
- 첫 사용자가 `반영 취소` 의미를 추가 설명 없이 이해하는 비율 증가
- 파일 업로드 후 저장/취소 액션에서 질문 발생 빈도 감소

가능하면 이후 아래 이벤트도 추적한다.

- 다운로드 화면 진입 후 첫 클릭 버튼 종류
- 업로드 상세 화면 체류 시간
- 반영 전 파일 삭제 vs 반영 후 취소 사용 비율

---

## 10. 기술 메모

- 가능하면 기존 라우트와 API는 유지하고, 템플릿 문구/레이아웃 중심으로 변경한다.
- 백엔드 데이터 구조나 매칭 규칙은 이번 작업에서 건드리지 않는다.
- 기존 CSV/XLSX 다운로드 엔드포인트는 호환성 때문에 유지한다.
- 상태값 내부 코드는 그대로 두고, 사용자 노출 라벨만 바꾸는 방식도 허용한다.

---

## 11. 완료 정의

아래 조건을 모두 만족하면 완료로 본다.

1. 대시보드에서 핵심 재고 정보만 전면 노출된다.
2. 다운로드 화면의 기본 버튼은 1개다.
3. 부가 다운로드는 숨김 또는 2차 액션으로 이동한다.
4. 업로드/상세 화면의 핵심 용어가 쉬운 말로 통일된다.
5. 기존 업로드, 반영, 반영 취소, 다운로드 기능은 그대로 동작한다.
